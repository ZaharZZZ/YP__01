.8086
.model small
.stack 100h

.data
stroka db 'Assembler'       ; исходная строка
revers db 9 dup(' ')        ; строка для результата (9 пробелов)
len equ 9                   ; длина строки

.code
Start:
    mov ax, @data
    mov ds, ax
    
    ; Загружаем символы строки в стек
    mov cx, len             ; счетчик = длина строки
    mov si, 0               ; индекс для stroka
    
load_to_stack:
    mov al, stroka[si]      ; загружаем символ из stroka
    push ax                 ; помещаем символ в стек
    inc si                  ; переходим к следующему символу
    loop load_to_stack      ; повторяем для всех символов
    
    ; Извлекаем символы из стека в обратном порядке
    mov cx, len             ; счетчик = длина строки
    mov di, 0               ; индекс для revers
    
pop_from_stack:
    pop ax                  ; извлекаем символ из стека
    mov revers[di], al      ; записываем в revers
    inc di                  ; переходим к следующей позиции
    loop pop_from_stack     ; повторяем для всех символов
    
    ; Завершение программы
    mov ah, 4Ch
    int 21h
end Start

Объяснение работы программы:

1.	Инициализация сегментов данных:
o	stroka содержит исходную строку "Assembler"
o	revers инициализируется 9 пробелами (длина "Assembler" + завершающий нуль в оригинальном задании, но в данной задаче просто 9 символов)

2.	Загрузка символов в стек:
o	Используем цикл load_to_stack для последовательной загрузки всех символов из stroka в стек
o	Символы загружаются в порядке: 'A', 's', 's', 'e', 'm', 'b', 'l', 'e', 'r'

3.	Извлечение символов из стека:
o	Используем цикл pop_from_stack для извлечения символов из стека
o	Так как стек работает по принципу LIFO (Last In, First Out), символы извлекаются в обратном порядке: 'r', 'e', 'l', 'b', 'm', 'e', 's', 's', 'A'

4.	Результат:
o	В переменной revers будет содержаться строка "relbmes sA" (если считать пробелы) или "relbme ssA" в зависимости от трактовки задания
o	Если в задании подразумевается только реверс без пробелов, то нужно изменить инициализацию revers
Примечание: В вашем примере переменная revers инициализирована 9 пробелами, поэтому результат будет содержать пробелы. Если нужен чистый реверс строки, можно инициализировать revers как db 9 dup(?).
